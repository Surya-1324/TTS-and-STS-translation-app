<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
    <style name="AppSearchView" parent="Widget.AppCompat.SearchView">
        <!-- Other SearchView attributes -->
        <item name="android:textColorHint">#FFAABBCC</item>
        <item name="android:editTextColor">@color/blue</item><!-- Change the color code as needed -->
    </style>
</resources>